describe('NRF', function () {
    it('URL sonunda /nrf olacak', () => { });

    describe('General', function () { // section
        it('Altında 10 satır olur', () => { });
        it('Hepsi enabled olur', () => { });

        describe('ConnectionTimeout', function () {
            it('edit düğmesine tıklanılır', () => { });
            it('metin kutusuna tıklanılır', () => { });
            
            describe('geçerli değerle güncellenir', function () {
                it('yeni değer 1-10 arasında girilir', () => { });
                it('güncelle düğmesi aktiftir', () => { });
                it('güncelle düğmesine basıldığında "başarılı" mesajı görünür', () => { });
                it('güncelle düğmesine basıldığında "başarılı" mesajı görünür', () => { });
            });

            describe('geçersiz değer hata üretir', function () {
            });

        });

        describe('SUITE 3', function () {
            it('it 3', () => { });
        });

    });